import 'package:flutter/material.dart';

// void main() {
//   runApp(MyApp());
// }

class test2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('Blank Screen')),
        body: Center(child: Text('')),
      ),
    );
  }
}
